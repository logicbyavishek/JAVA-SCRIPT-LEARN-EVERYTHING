// What is Dom ? How does it represent the HTML structure ?
// Name the types of nodes in DOM tree ?
// whats the difference between element node and text node ?
// inspect the following HTML structure in the browser and identify each node?
// what is the difference between document.getElementById and document.querySelector ?
// what does getElementsByClassName return ? Is it an array ? how to convert it into an array ?
// use querySelectorAll to secelct buttons with class ".buy-now"